# MCP server downloaded by setup script into the repo ROOT (anchored so the
# vendored revit-bim/mcp-server/ that ships with the plugin is NOT ignored).
/mcp-server/

# Build artifacts
dist/
*.zip

# Tools downloaded by setup
.tools/

# IDE
.idea/
.vscode/
*.swp

# OS
Thumbs.db
.DS_Store
